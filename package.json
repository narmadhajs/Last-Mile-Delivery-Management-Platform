{
  "name": "last-mile-delivery-tracker",
  "version": "1.0.0",
  "description": "Enterprise-grade Last-Mile Delivery Management Platform with dynamic rate calculation, intelligent agent auto-assignment, and immutable tracking history.",
  "main": "server/src/index.ts",
  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"blue,green\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "seed": "npm run seed --prefix server",
    "test": "npm run test --prefix server",
    "package:zip": "node scripts/package-deliverable.js"
  },
  "keywords": [
    "delivery-tracker",
    "last-mile-logistics",
    "rate-calculation-engine",
    "auto-assignment",
    "audit-trail"
  ],
  "author": "LogiTrack Solutions",
  "license": "MIT",
  "devDependencies": {
    "archiver": "^7.0.1",
    "concurrently": "^9.1.2"
  }
}
